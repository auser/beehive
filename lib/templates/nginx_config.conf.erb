upstream <%= app_name %>_upstream {
  server 127.0.0.1:5000;
}

server {
  listen        80;
  server_name   <%= app_name %>;
  access_log    <%= prefix %>/logs/<%= app_name %>.log;
  
  location / {
    proxy_pass http://<%= app_name %>_upstream;
  }
  
}